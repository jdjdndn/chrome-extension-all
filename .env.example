# 环境变量配置示例
# 复制此文件为 .env.local 或直接设置环境变量

# 热重载开关（开发模式自动启用）
HOT_RELOAD=true

# 调试模式
DEBUG=false

# 构建分析
ANALYZE=false

# 使用示例:
# HOT_RELOAD=true npm run build
# 或在 package.json 中:
# "scripts": {
#   "build:dev": "HOT_RELOAD=true vite build"
# }
