# 资源加速器优化计划

> 基线版本：v21（Worker and cache optimizations）
> 创建时间：2026-05-09
> 规划周期：Q2-Q3 2026

---

## 一、当前状态评估

### 已完成功能（v1-v21）

| 版本 | 功能模块 | 状态 |
|------|---------|------|
| v1-v5 | 基础资源拦截和替换 | ✅ 稳定 |
| v6-v8 | API拦截、即时处理、图片压缩 | ✅ 稳定 |
| v9-v12 | 智能预加载、性能监控 | ✅ 稳定 |
| v13-v16 | 自适应压缩、SVG优化 | ✅ 稳定 |
| v17-v19 | Worker压缩、优先级队列 | ✅ 稳定 |
| v20-v21 | Worker预热、健康检查、内存管理 | ✅ 已实现 |

### 技术栈

- **构建工具**：Vite + esbuild
- **开发环境**：热重载（HTTP轮询 + WebSocket）
- **代码质量**：Prettier + ESLint + TypeScript
- **测试框架**：Vitest

### 存在问题

| 问题类型 | 具体问题 | 影响 |
|---------|---------|------|
| 性能 | 位置感知加载未完全实现 | 视口外资源加载过多 |
| 稳定性 | Worker崩溃后恢复不完整 | 功能中断 |
| 内存 | 大图片缓存策略不完善 | 内存占用过高 |
| 兼容性 | 严格CSP网站部分功能失效 | 用户体验差 |
| 可维护性 | 代码耦合度高，单元测试覆盖率低 | 维护成本高 |

---

## 二、优化目标

### 性能目标

- LCP优化：提升 15-20%
- 内存占用：降低 30%
- CPU使用率：降低 20%
- 加载成功率：提升至 98%

### 质量目标

- 单元测试覆盖率：> 80%
- E2E测试覆盖：核心流程全覆盖
- 代码重复率：< 5%
- 文档完整度：100%

---

## 三、优化路线图

### Phase 1：稳定性优化（2周）

#### 1.1 Worker池健康检查增强

**目标**：确保Worker崩溃后完全恢复

**实施内容**：
- Worker生命周期管理
- 崩溃自动重启机制
- 任务失败重试队列
- 健康状态监控面板

**验证标准**：
- Worker崩溃后5秒内自动恢复
- 任务不丢失
- 日志完整记录

#### 1.2 错误边界和降级策略

**目标**：异常情况下保证核心功能可用

**实施内容**：
- 全局错误捕获
- 功能模块降级开关
- 用户提示机制
- 错误上报系统

**验证标准**：
- 单模块失败不影响整体
- 降级过程无闪烁
- 错误可追溯

#### 1.3 内存泄漏检测和修复

**目标**：长时间运行无内存增长

**实施内容**：
- 内存使用监控
- 定时清理机制
- 缓存淘汰策略优化
- 泄漏检测工具集成

**验证标准**：
- 24小时运行内存增长 < 10MB
- 缓存命中率 > 80%
- GC频率稳定

---

### Phase 2：性能优化（3周）

#### 2.1 位置感知加载完善

**目标**：根据资源位置智能加载

**实施内容**：
- 视口检测优化（IntersectionObserver）
- 距离优先级队列
- 动态阈值调整
- 滚动预测加载

**验证标准**：
- 视口外资源延迟加载率 > 90%
- 视口内资源优先级提升明显
- 滚动流畅度无影响

#### 2.2 缓存策略优化

**目标**：平衡内存占用和命中率

**实施内容**：
- LRU缓存淘汰算法
- 大小分级缓存
- 持久化缓存策略
- 缓存预热机制

**验证标准**：
- 缓存命中率 > 85%
- 内存占用降低 30%
- 缓存查询延迟 < 1ms

#### 2.3 Worker性能优化

**目标**：提升压缩吞吐量

**实施内容**：
- Worker池动态扩容
- 任务分片处理
- SIMD加速（可选）
- 结果缓存复用

**验证标准**：
- 压缩吞吐量提升 50%
- Worker利用率 > 80%
- 平均压缩时间降低 30%

---

### Phase 3：功能增强（3周）

#### 3.1 智能资源识别

**目标**：更准确的资源类型判断

**实施内容**：
- MIME类型检测增强
- 内容嗅探机制
- 资源重要性评分
- 智能处理策略选择

**验证标准**：
- 资源识别准确率 > 95%
- 误判率 < 2%
- 处理策略匹配度 > 90%

#### 3.2 CDN源扩展

**目标**：支持更多CDN源

**实施内容**：
- 新增国内主流CDN映射
- 自动CDN探测
- 健康检查机制
- 故障自动切换

**验证标准**：
- 支持 20+ CDN源
- 故障切换时间 < 100ms
- CDN可用性监控

#### 3.3 用户配置界面

**目标**：可自定义优化策略

**实施内容**：
- Popup配置界面
- 站点级规则配置
- 性能数据展示
- 一键恢复默认

**验证标准**：
- 配置实时生效
- 界面响应流畅
- 配置持久化

---

### Phase 4：质量提升（2周）

#### 4.1 单元测试补充

**目标**：核心功能测试覆盖率 > 80%

**实施内容**：
- 核心模块单元测试
- 边界条件测试
- 异常流程测试
- Mock数据集

**验证标准**：
- 测试覆盖率 > 80%
- 所有关键路径有测试
- CI集成自动化测试

#### 4.2 E2E测试建设

**目标**：核心用户流程自动化测试

**实施内容**：
- 关键场景测试用例
- 多浏览器兼容测试
- 性能回归测试
- 视觉回归测试（可选）

**验证标准**：
- 核心流程全覆盖
- 自动化执行
- 回归检测有效

#### 4.3 文档完善

**目标**：开发和使用文档完整

**实施内容**：
- API文档生成
- 架构设计文档
- 用户使用指南
- 故障排查手册

**验证标准**：
- 文档覆盖所有功能
- 示例代码可运行
- 定期更新维护

---

### Phase 5：架构重构（可选，4周）

#### 5.1 模块化重构

**目标**：降低耦合，提升可维护性

**实施内容**：
- 插件化架构设计
- 核心引擎抽取
- 功能模块独立
- 依赖注入机制

**验证标准**：
- 模块边界清晰
- 单模块可独立测试
- 扩展性强

#### 5.2 TypeScript迁移

**目标**：类型安全，提升开发体验

**实施内容**：
- 核心模块TS重写
- 类型定义补充
- 构建流程优化
- 类型测试

**验证标准**：
- 类型覆盖率 > 90%
- 编译无错误
- IDE智能提示完整

---

## 四、技术方案

### 4.1 性能监控方案

```javascript
// 性能指标采集
const metrics = {
  // 资源加载性能
  resourceTiming: {
    dns: 0,
    tcp: 0,
    request: 0,
    response: 0
  },

  // 处理性能
  processing: {
    compressTime: 0,
    cacheHitRate: 0,
    workerUtilization: 0
  },

  // 内存性能
  memory: {
    usedJSHeapSize: 0,
    totalJSHeapSize: 0,
    cacheSize: 0
  }
}
```

### 4.2 错误处理方案

```javascript
// 全局错误边界
class ErrorHandler {
  constructor() {
    this.fallbackStrategies = new Map()
  }

  // 注册降级策略
  register(module, fallback) {
    this.fallbackStrategies.set(module, fallback)
  }

  // 处理错误
  handle(error, module) {
    const fallback = this.fallbackStrategies.get(module)
    if (fallback) {
      return fallback(error)
    }
    // 默认降级：禁用该模块
    this.disableModule(module)
  }
}
```

### 4.3 缓存淘汰方案

```javascript
// LRU缓存实现
class LRUCache {
  constructor(maxSize) {
    this.maxSize = maxSize
    this.cache = new Map()
  }

  get(key) {
    if (!this.cache.has(key)) return null
    // 访问时移到末尾（最近使用）
    const value = this.cache.get(key)
    this.cache.delete(key)
    this.cache.set(key, value)
    return value
  }

  set(key, value) {
    if (this.cache.has(key)) {
      this.cache.delete(key)
    }
    if (this.cache.size >= this.maxSize) {
      // 删除最旧的（第一个）
      this.cache.delete(this.cache.keys().next().value)
    }
    this.cache.set(key, value)
  }
}
```

---

## 五、风险和缓解

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| Worker兼容性问题 | 功能中断 | 中 | 渐进式降级，主线程备份 |
| 内存占用过高 | 浏览器卡顿 | 中 | 严格内存监控，主动释放 |
| CSP限制 | 部分功能失效 | 高 | 功能检测，替代方案 |
| 性能回归 | 用户体验差 | 低 | 自动化性能测试 |
| 第三方库更新 | 兼容性问题 | 低 | 锁定版本，定期更新 |

---

## 六、验收标准

### 性能指标

- [ ] LCP提升 15-20%
- [ ] 内存占用降低 30%
- [ ] CPU使用率降低 20%
- [ ] 加载成功率 > 98%

### 质量指标

- [ ] 单元测试覆盖率 > 80%
- [ ] E2E测试核心流程全覆盖
- [ ] 代码重复率 < 5%
- [ ] 文档完整度 100%

### 稳定性指标

- [ ] Worker崩溃恢复时间 < 5s
- [ ] 24小时运行无内存泄漏
- [ ] 错误捕获率 100%
- [ ] 降级成功率 100%

---

## 七、时间规划

| 阶段 | 时间 | 人力 | 主要产出 |
|------|------|------|---------|
| Phase 1 | 2周 | 1人 | 稳定性优化完成 |
| Phase 2 | 3周 | 1人 | 性能优化完成 |
| Phase 3 | 3周 | 1人 | 功能增强完成 |
| Phase 4 | 2周 | 1人 | 质量提升完成 |
| Phase 5 | 4周 | 1人 | 架构重构（可选）|

**总计**：10-14周（根据Phase 5是否执行）

---

## 八、后续规划

### Q4 2026

- AI驱动的资源优化
- 多浏览器支持（Firefox、Edge）
- 云端配置同步
- 社区版本发布

### 2027 H1

- WebAssembly压缩加速
- Service Worker离线优化
- 跨设备资源预加载
- 性能分析平台

---

## 附录

### A. 参考文档

- [Manifest V3 Worker 创建模式](memory/manifest-v3-worker-pattern.md)
- [位置感知加载设计](docs/superpowers/specs/2026-05-07-position-aware-loading-design.md)
- [工程化改进计划](docs/superpowers/plans/2026-05-07-engineering-improvements.md)

### B. 相关Issue

- Worker创建失败修复 (be2217a)
- 位置感知加载实现中
- 内存泄漏待优化

### C. 技术债务

- [ ] 资源加速器代码耦合度高
- [ ] 单元测试覆盖率不足
- [ ] 文档不完整
- [ ] TypeScript迁移未完成
