{
  "name": "chrome-extension-template",
  "version": "1.0.0",
  "description": "Chrome 扩展 - 减少资源使用",
  "private": true,
  "scripts": {
    "watch": "node scripts/watch-and-sync.js",
    "sync": "node scripts/watch-and-sync.js --once",
    "build": "node scripts/watch-and-sync.js --once"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
