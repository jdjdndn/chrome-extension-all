{"id":"ch-2026-04-25T13:30:45.190Z-r3nv6s","taskId":"turn-13","filePath":"E:\\code\\chrome-extension-template\\content\\entries\\core.js","action":"modify","before":"// 9. ContentBridge\nimport '../utils/content-bridge.js';\n\n// 10. Core modules\nimport '../core/store.js';","after":"// 9. ContentBridge\nimport '../utils/content-bridge.js';\n\n// 10. 资源加速器（最优先加载 — document_start 时尽早拦截资源）\nimport '../../shared/cdn-mappings.js';\nimport '../modules/js-replacer.js';\nimport '../modules/font-replacer.js';\nimport '../modules/css-accelerator.js';\nimport '../modules/image-optimizer.js';\nimport '../modules/resource-preloader.js';\nimport '../modules/resource-deduplicator.js';\nimport '../modules/resource-accelerator.js';\n\n// 11. Core modules\nimport '../core/store.js';","timestamp":1777123845196,"rolledBack":false}
{"id":"ch-2026-04-25T13:31:01.332Z-twr5sa","taskId":"turn-15","filePath":"E:\\code\\chrome-extension-template\\content\\entries\\core.js","action":"modify","before":"// 13. 资源加速器模块（依赖顺序：CDNMappings → 子模块 → 主模块）\nimport '../../shared/cdn-mappings.js';\nimport '../modules/js-replacer.js';\nimport '../modules/font-replacer.js';\nimport '../modules/css-accelerator.js';\nimport '../modules/image-optimizer.js';\nimport '../modules/resource-preloader.js';\nimport '../modules/resource-deduplicator.js';\nimport '../modules/resource-accelerator.js';\n\n// 14. Main entry","after":"// 14. Main entry","timestamp":1777123861339,"rolledBack":false}
{"id":"ch-2026-04-25T13:31:10.747Z-80ixua","taskId":"turn-16","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"  // 根据文档状态选择初始化时机\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', autoInit);\n  } else {\n    // 延迟初始化，确保其他模块已加载\n    setTimeout(autoInit, 0);\n  }","after":"  // 立即初始化 — document_start 时 MutationObserver 能捕获所有后续DOM变化\n  autoInit();","timestamp":1777123870752,"rolledBack":false}
{"id":"ch-2026-04-25T13:31:34.353Z-d2kh85","taskId":"turn-18","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    async init() {\n      if (!this.config.enabled) {\n        console.log(`${LOG_PREFIX} 模块已禁用`);\n        return;\n      }\n\n      // 1. 加载配置\n      await this.loadConfig();\n\n      // 2. 加载缓存\n      await this.loadCache();\n\n      // 3. 先用缓存替换页面中已存在的资源\n      this._applyCacheToPage();\n\n      // 4. 后台探测CDN健康(不阻塞初始化)\n      this._probeCDNHealth();\n\n      // 5. 初始化子模块\n      this.initModules();\n\n      // 6. 监听统计消息\n      this.listenStats();\n\n      // 7. 加载累计统计\n      await this._loadCumulativeStats();\n\n      ","after":"    async init() {\n      if (!this.config.enabled) {\n        console.log(`${LOG_PREFIX} 模块已禁用`);\n        return;\n      }\n\n      // 1. 立即初始化子模块（设置MutationObserver/createElement拦截，最优先）\n      this.initModules();\n\n      // 2. 加载配置+缓存（异步，不阻塞observer）\n      await Promise.all([\n        this.loadConfig(),\n        this.loadCache()\n      ]);\n\n      // 3. 用缓存替换页面中已存在的资源\n      this._applyCacheToPage();\n\n      // 4. 后台探测CDN健康(不阻塞)\n      this._probeCDNHealth();\n\n      // 5. 监听统计消息\n      this.listenStats();\n\n ","timestamp":1777123894359,"rolledBack":false}
{"id":"ch-2026-04-25T13:33:07.200Z-xuw63x","taskId":"turn-23","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    async init() {\n      if (!this.config.enabled) {\n        console.log(`${LOG_PREFIX} 模块已禁用`);\n        return;\n      }\n\n      // 1. 立即初始化子模块（设置MutationObserver/createElement拦截，最优先）\n      this.initModules();\n\n      // 2. 加载配置+缓存（异步，不阻塞observer）\n      await Promise.all([\n        this.loadConfig(),\n        this.loadCache()\n      ]);\n\n      // 3. 用缓存替换页面中已存在的资源\n      this._applyCacheToPage();\n\n      // 4. 后台探测CDN健康(不阻塞)\n      this._probeCDNHealth();\n\n      // 5. 监听统计消息\n      this.listenStats();\n\n ","after":"    async init() {\n      if (!this.config.enabled) {\n        console.log(`${LOG_PREFIX} 模块已禁用`);\n        return;\n      }\n\n      // 1. 加载配置（快速，chrome.storage.local 本地读取）\n      await this.loadConfig();\n      if (!this.config.enabled) return;\n\n      // 2. 立即初始化子模块（设置MutationObserver/createElement拦截）\n      this.initModules();\n\n      // 3. 加载缓存（异步，不阻塞observer）\n      await this.loadCache();\n\n      // 4. 用缓存替换页面中已存在的资源\n      this._applyCacheToPage();\n\n      // 5. 后台探测CDN健康(不阻塞)\n      this._probeCDNHeal","timestamp":1777123987208,"rolledBack":false}
{"id":"ch-2026-04-25T13:39:13.447Z-1lrpfz","taskId":"turn-37","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"      // 匹配CDN映射\n      const match = window.CDNMappings?.matchJSLibrary(url);\n      if (!match) {\n        this.stats.skipped++;\n        return;\n      }\n\n      // 执行替换\n      try {\n        const originalSrc = script.src;\n        script.src = match.cdnUrl;\n\n        this.stats.replaced++;\n        console.log(`${LOG_PREFIX} 替换: ${match.name}`);\n        console.log(`  原始: ${originalSrc}`);\n        console.log(`  CDN: ${match.cdnUrl}`);\n\n        // 上报统计\n        this.reportReplacement(match);\n\n        /","after":"      // 匹配CDN映射\n      const match = window.CDNMappings?.matchJSLibrary(url);\n      if (!match) {\n        this.stats.skipped++;\n        return;\n      }\n\n      // CSP检测：页面禁止外部脚本时跳过替换\n      if (this._isCSPRestricted()) {\n        this.stats.skipped++;\n        console.log(`${LOG_PREFIX} 跳过(CSP限制): ${url}`);\n        return;\n      }\n\n      // 执行替换 + 失败回退\n      try {\n        const originalSrc = script.src;\n        script.src = match.cdnUrl;\n\n        // 替换失败时回退原URL\n        script.onerror = () => {\n     ","timestamp":1777124353453,"rolledBack":false}
{"id":"ch-2026-04-25T13:39:30.715Z-v41d15","taskId":"turn-38","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"    /**\n     * 检查是否为目标CDN","after":"    /**\n     * CSP检测：页面meta标签中CSP是否限制外部脚本\n     */\n    _isCSPRestricted() {\n      if (this._cspRestricted !== undefined) return this._cspRestricted;\n\n      const metaCSP = document.querySelector('meta[http-equiv=\"Content-Security-Policy\"]');\n      if (!metaCSP) {\n        this._cspRestricted = false;\n        return false;\n      }\n\n      const policy = metaCSP.getAttribute('content') || '';\n      // 检查script-src是否只允许self/内联\n      const scriptSrc = policy.match(/script-src\\s+([^;]+)/);\n      if (scr","timestamp":1777124370721,"rolledBack":false}
{"id":"ch-2026-04-25T13:40:03.909Z-9ch5oc","taskId":"turn-40","filePath":"E:\\code\\chrome-extension-template\\shared\\cdn-mappings.js","action":"modify","before":"    {\n      id: 'baomitu',\n      name: '360前端',\n      baseUrl: 'https://cdn.baomitu.com/ajax/libs/',\n      format: 'bootcdn'\n    },","after":"    {\n      id: 'baomitu',\n      name: '360前端(证书异常，降级)',\n      baseUrl: 'https://cdn.baomitu.com/ajax/libs/',\n      format: 'bootcdn',\n      _disabled: true // SSL证书过期 ERR_CERT_DATE_INVALID\n    },","timestamp":1777124403915,"rolledBack":false}
{"id":"ch-2026-04-25T13:40:48.162Z-fx7nzi","taskId":"turn-46","filePath":"E:\\code\\chrome-extension-template\\shared\\cdn-mappings.js","action":"modify","before":"for (const cdnId of ordered) {\n      const cdn = CDN_BY_ID[cdnId];\n      if (!cdn) continue;\n      const url = buildCDNUrl(cdn, config, version, config.file);","after":"for (const cdnId of ordered) {\n      const cdn = CDN_BY_ID[cdnId];\n      if (!cdn || cdn._disabled) continue;\n      const url = buildCDNUrl(cdn, config, version, config.file);","timestamp":1777124448168,"rolledBack":false}
{"id":"ch-2026-04-25T13:40:54.207Z-bh4xc4","taskId":"turn-47","filePath":"E:\\code\\chrome-extension-template\\shared\\cdn-mappings.js","action":"modify","before":"    for (const cdnId of cdnOrder) {\n      const cdn = CDN_BY_ID[cdnId];\n      if (!cdn) continue;","after":"    for (const cdnId of cdnOrder) {\n      const cdn = CDN_BY_ID[cdnId];\n      if (!cdn || cdn._disabled) continue;","timestamp":1777124454213,"rolledBack":false}
{"id":"ch-2026-04-25T13:41:14.904Z-ecd1lj","taskId":"turn-48","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"      // 执行替换 + 失败回退\n      try {\n        const originalSrc = script.src;\n        script.src = match.cdnUrl;\n\n        // 替换失败时回退原URL\n        script.onerror = () => {\n          console.warn(`${LOG_PREFIX} CDN加载失败，回退原URL: ${match.cdnUrl}`);\n          this._processedScripts.delete(script);\n          script.src = originalSrc;\n          this.stats.errors++;\n        };\n\n        this.stats.replaced++;\n        console.log(`${LOG_PREFIX} 替换: ${match.name}`);\n        console.log(`  原始: ${originalSrc}`);\n  ","after":"      // 执行替换 + CDN降级链 + 最终回退\n      try {\n        const originalSrc = script.src;\n        // 构建降级链: 主CDN → fallback CDNs → 原URL\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);\n\n        script.src = match.cdnUrl;\n        this.stats.replaced++;\n        console.log(`${LOG_PREFIX} 替换: ${match.name}`);\n        console.log(`  原始: ${originalSrc}`);\n        console.log(`  CDN: ${match.cdnUrl}`);\n\n        // CDN降级：失败后尝试下一个CDN，全部失败回退原URL\n        script.onerror = () => {\n          if ","timestamp":1777124474911,"rolledBack":false}
{"id":"ch-2026-04-25T13:42:24.955Z-1ed13a","taskId":"turn-52","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\font-replacer.js","action":"modify","before":"      // 执行替换\n      try {\n        const originalHref = link.href;\n        link.href = match.cdnUrl;\n\n        this.stats.replaced++;\n        console.log(`${LOG_PREFIX} 替换: ${match.name}`);\n        console.log(`  原始: ${originalHref}`);\n        console.log(`  CDN: ${match.cdnUrl}`);\n\n        // 上报统计\n        this.reportReplacement(match);\n\n        // 记录详情\n        this.stats.details.push({\n          name: match.name,\n          original: originalHref,\n          cdn: match.cdnUrl,\n          time: Date.","after":"      // 执行替换 + CDN降级链 + 最终回退\n      try {\n        const originalHref = link.href;\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);\n\n        link.href = match.cdnUrl;\n\n        // CDN降级：失败后尝试下一个CDN，全部失败回退原URL\n        link.onerror = () => {\n          if (fallbacks.length > 0) {\n            const nextCdn = fallbacks.shift();\n            console.log(`${LOG_PREFIX} CDN失败，尝试降级: ${nextCdn}`);\n            link.href = nextCdn;\n          } else {\n            console.warn(`${LOG_PREFIX}","timestamp":1777124544960,"rolledBack":false}
{"id":"ch-2026-04-25T13:42:41.942Z-bts2x0","taskId":"turn-53","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\css-accelerator.js","action":"modify","before":"      try {\n        const originalHref = link.href;\n        link.href = match.cdnUrl;\n\n        this.stats.replaced++;\n        console.log(`${LOG_PREFIX} 替换: ${match.name}`);\n        console.log(`  原始: ${originalHref}`);\n        console.log(`  CDN: ${match.cdnUrl}`);\n\n        this._reportReplacement(match);\n\n        this.stats.details.push({\n          name: match.name,\n          original: originalHref,\n          cdn: match.cdnUrl,\n          time: Date.now()\n        });\n      } catch (error) {\n   ","after":"      try {\n        const originalHref = link.href;\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);\n\n        link.href = match.cdnUrl;\n\n        // CDN降级：失败后尝试下一个CDN，全部失败回退原URL\n        link.onerror = () => {\n          if (fallbacks.length > 0) {\n            const nextCdn = fallbacks.shift();\n            console.log(`${LOG_PREFIX} CDN失败，尝试降级: ${nextCdn}`);\n            link.href = nextCdn;\n          } else {\n            console.warn(`${LOG_PREFIX} 所有CDN失败，回退原URL: ${originalHre","timestamp":1777124561947,"rolledBack":false}
{"id":"ch-2026-04-25T13:49:17.572Z-j8qlpn","taskId":"turn-62","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    /**\n     * 监听统计消息和配置更新\n     */\n    listenStats() {","after":"    /**\n     * 监听统计消息和配置更新\n     */\n    listenStats() {\n      // 全局CSP检测 — 检测到违规立即禁用所有替换\n      this._setupCSPGuard();\n\n      if (typeof chrome !== 'undefined' && chrome.runtime?.onMessage) {","timestamp":1777124957577,"rolledBack":false}
{"id":"ch-2026-04-25T13:49:42.366Z-8v0gpf","taskId":"turn-63","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    /**\n     * 监听统计消息和配置更新\n     */","after":"    /**\n     * CSP检测：监听securitypolicyviolation事件\n     * 检测到CDN URL违规时立即禁用所有资源替换\n     */\n    _setupCSPGuard() {\n      if (this._cspListener) return;\n\n      this._cspListener = (event) => {\n        const url = event.blockedURL || '';\n        const directive = event.violatedDirective || '';\n        // 仅关注script-src/style-src相关违规\n        if (!directive.includes('script') && !directive.includes('style') && !directive.includes('connect')) return;\n        // 仅关注CDN URL（排除页面自身资源）\n        const cdnDomain","timestamp":1777124982372,"rolledBack":false}
{"id":"ch-2026-04-25T13:49:58.515Z-4q8vo2","taskId":"turn-64","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"      // CSP检测：页面禁止外部脚本时跳过替换\n      if (this._isCSPRestricted()) {\n        this.stats.skipped++;\n        console.log(`${LOG_PREFIX} 跳过(CSP限制): ${url}`);\n        return;\n      }","after":"      // CSP检测：全局标记(由ResourceAccelerator通过securitypolicyviolation设置)\n      if (window.resourceAcceleratorInstance?._cspRestricted) {\n        this.stats.skipped++;\n        return;\n      }","timestamp":1777124998520,"rolledBack":false}
{"id":"ch-2026-04-25T13:50:16.457Z-tu1c5d","taskId":"turn-65","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"    /**\n     * CSP检测：页面meta标签中CSP是否限制外部脚本\n     */\n    _isCSPRestricted() {\n      if (this._cspRestricted !== undefined) return this._cspRestricted;\n\n      const metaCSP = document.querySelector('meta[http-equiv=\"Content-Security-Policy\"]');\n      if (!metaCSP) {\n        this._cspRestricted = false;\n        return false;\n      }\n\n      const policy = metaCSP.getAttribute('content') || '';\n      // 检查script-src是否只允许self/内联\n      const scriptSrc = policy.match(/script-src\\s+([^;]+)/);\n      if (scr","after":"    /**\n     * 检查是否为目标CDN","timestamp":1777125016462,"rolledBack":false}
{"id":"ch-2026-04-25T13:50:24.370Z-hz1iag","taskId":"turn-66","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-preloader.js","action":"modify","before":"          const priorityCDNs = ['bootcdn', 'baomitu', 'staticfile', 'jsdelivr'];","after":"          const priorityCDNs = ['bootcdn', 'staticfile', 'jsdelivr'];","timestamp":1777125024375,"rolledBack":false}
{"id":"ch-2026-04-25T13:50:49.021Z-q0la27","taskId":"turn-67","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-preloader.js","action":"modify","before":"    /**\n     * 添加preload提示\n     */\n    _addPreload(url, asType) {\n      if (this._processedUrls.has(url)) return;\n      if (this.stats.preloaded >= this.maxPreloads) {\n        this.stats.skipped++;\n        return;\n      }","after":"    /**\n     * 添加preload提示\n     */\n    _addPreload(url, asType) {\n      if (this._processedUrls.has(url)) return;\n      if (this.stats.preloaded >= this.maxPreloads) {\n        this.stats.skipped++;\n        return;\n      }\n\n      // 跳过CDN域名URL（已被替换模块处理，preload无意义且可能触发CSP）\n      if (this._isCDNUrl(url)) {\n        this.stats.skipped++;\n        return;\n      }\n\n      // CSP已检测到限制时跳过外部URL\n      if (window.resourceAcceleratorInstance?._cspRestricted) {\n        this.stats.skipped++;\n        return;\n   ","timestamp":1777125049026,"rolledBack":false}
{"id":"ch-2026-04-25T13:51:27.225Z-1qgdxv","taskId":"turn-69","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-preloader.js","action":"modify","before":"    _isExcluded(url) {\n      const defaults = [/^chrome-extension:/i, /^moz-extension:/i, /^about:/i];\n      if (defaults.some(p => p.test(url))) return true;\n      return this.excludePatterns.some(p => p.test(url));\n    }","after":"    _isExcluded(url) {\n      const defaults = [/^chrome-extension:/i, /^moz-extension:/i, /^about:/i];\n      if (defaults.some(p => p.test(url))) return true;\n      return this.excludePatterns.some(p => p.test(url));\n    }\n\n    /**\n     * 检查URL是否属于CDN域名\n     */\n    _isCDNUrl(url) {\n      if (!window.CDNMappings?.CDN_SOURCES) return false;\n      return window.CDNMappings.CDN_SOURCES.some(cdn => {\n        try {\n          return url.includes(new URL(cdn.baseUrl).hostname);\n        } catch { return ","timestamp":1777125087230,"rolledBack":false}
{"id":"ch-2026-04-25T13:52:08.510Z-yb5v0b","taskId":"turn-71","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-preloader.js","action":"modify","before":"      window.CDNMappings.CDN_SOURCES.forEach(cdn => {","after":"      window.CDNMappings.CDN_SOURCES.forEach(cdn => {\n        // 跳过disabled的CDN（如证书过期的）\n        if (cdn._disabled) return;","timestamp":1777125128516,"rolledBack":false}
{"id":"ch-2026-04-25T13:54:39.713Z-cwsucj","taskId":"turn-79","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"      this._setupCSPGuard();\n\n      if (typeof chrome !== 'undefined' && chrome.runtime?.onMessage) {\n      if (typeof chrome !== 'undefined' && chrome.runtime?.onMessage) {\n        chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {","after":"      this._setupCSPGuard();\n\n      if (typeof chrome !== 'undefined' && chrome.runtime?.onMessage) {\n        chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {","timestamp":1777125279719,"rolledBack":false}
{"id":"ch-2026-04-25T14:00:47.906Z-hxbq45","taskId":"turn-86","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    async init() {\n      if (!this.config.enabled) {\n        console.log(`${LOG_PREFIX} 模块已禁用`);\n        return;\n      }\n\n      // 1. 加载配置（快速，chrome.storage.local 本地读取）\n      await this.loadConfig();\n      if (!this.config.enabled) return;\n\n      // 2. 立即初始化子模块（设置MutationObserver/createElement拦截）\n      this.initModules();\n\n      // 3. 加载缓存（异步，不阻塞observer）\n      await this.loadCache();\n\n      // 4. 用缓存替换页面中已存在的资源\n      this._applyCacheToPage();\n\n      // 5. 后台探测CDN健康(不阻塞)\n      this._probeCDNHeal","after":"    async init() {\n      if (!this.config.enabled) {\n        console.log(`${LOG_PREFIX} 模块已禁用`);\n        return;\n      }\n\n      // 1. 加载配置（快速，chrome.storage.local 本地读取）\n      await this.loadConfig();\n      if (!this.config.enabled) return;\n\n      // 2. CSP预检 — 如果页面禁止外部脚本，跳过所有替换\n      await this._precheckCSP();\n      if (this._cspRestricted) {\n        console.warn(`${LOG_PREFIX} 页面CSP禁止外部脚本，仅启用图片优化`);\n      }\n\n      // 3. 初始化子模块（CSP限制时跳过脚本/样式替换）\n      this.initModules();\n\n      // 4. 加载缓存\n      a","timestamp":1777125647914,"rolledBack":false}
{"id":"ch-2026-04-25T14:02:00.749Z-0tzyod","taskId":"turn-90","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    }\n\n    /**\n     * 加载配置\n     */","after":"    }\n\n    /**\n     * CSP预检：在替换前检测页面是否禁止加载外部CDN脚本\n     * 通过插入测试script触发securitypolicyviolation事件\n     */\n    async _precheckCSP() {\n      // 同步检查meta标签CSP\n      const metaCSP = document.querySelector('meta[http-equiv=\"Content-Security-Policy\"]');\n      if (metaCSP) {\n        const content = metaCSP.getAttribute('content') || '';\n        const scriptSrc = content.match(/script-src\\s+([^;]+)/);\n        if (scriptSrc && !/https?:|\\*\\./.test(scriptSrc[1])) {\n          this._cspRestricted = true;\n   ","timestamp":1777125720765,"rolledBack":false}
{"id":"ch-2026-04-25T14:02:31.959Z-xiessm","taskId":"turn-93","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"      const excludePatterns = this._buildExcludePatterns();\n\n      // JS替换模块\n      if (this.config.jsReplace && window.JSReplacer) {","after":"      const excludePatterns = this._buildExcludePatterns();\n      const cspBlock = !!this._cspRestricted;\n\n      // JS替换模块（CSP限制时跳过）\n      if (!cspBlock && this.config.jsReplace && window.JSReplacer) {","timestamp":1777125751969,"rolledBack":false}
{"id":"ch-2026-04-25T14:02:45.275Z-d0jreg","taskId":"turn-94","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"      // 字体替换模块\n      if (this.config.fontReplace && window.FontReplacer) {","after":"      // 字体替换模块（CSP限制时跳过）\n      if (!cspBlock && this.config.fontReplace && window.FontReplacer) {","timestamp":1777125765282,"rolledBack":false}
{"id":"ch-2026-04-25T14:03:07.563Z-rusekz","taskId":"turn-95","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"      // CSS加速模块\n      if (this.config.cssReplace && window.CSSAccelerator) {","after":"      // CSS加速模块（CSP限制时跳过）\n      if (!cspBlock && this.config.cssReplace && window.CSSAccelerator) {","timestamp":1777125787570,"rolledBack":false}
{"id":"ch-2026-04-25T14:03:23.335Z-e5yjk7","taskId":"turn-96","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"      // 资源预加载模块\n      if (this.config.preloadEnabled && window.ResourcePreloader) {","after":"      // 资源预加载模块（CSP限制时仅保留图片相关）\n      if (this.config.preloadEnabled && window.ResourcePreloader) {","timestamp":1777125803432,"rolledBack":false}
{"id":"ch-2026-04-25T14:03:41.173Z-8d1qy0","taskId":"turn-97","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"      // 资源去重模块\n      if (this.config.dedupEnabled && window.ResourceDeduplicator) {","after":"      // 资源去重模块（CSP限制时跳过脚本/样式去重）\n      if (this.config.dedupEnabled && window.ResourceDeduplicator) {","timestamp":1777125821179,"rolledBack":false}
{"id":"ch-2026-04-25T14:04:07.875Z-kdagot","taskId":"turn-98","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"        this.modules.preloader = new window.ResourcePreloader({\n          enabled: this.config.enabled,\n          preloadJS: this.config.jsReplace,\n          preloadCSS: this.config.cssReplace,\n          preloadFonts: this.config.fontReplace,","after":"        this.modules.preloader = new window.ResourcePreloader({\n          enabled: this.config.enabled,\n          preloadJS: !cspBlock && this.config.jsReplace,\n          preloadCSS: !cspBlock && this.config.cssReplace,\n          preloadFonts: !cspBlock && this.config.fontReplace,","timestamp":1777125847882,"rolledBack":false}
{"id":"ch-2026-04-25T14:04:16.697Z-j97b90","taskId":"turn-99","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"        this.modules.deduplicator = new window.ResourceDeduplicator({\n          enabled: this.config.enabled,\n          dedupJS: this.config.jsReplace,\n          dedupCSS: this.config.cssReplace,","after":"        this.modules.deduplicator = new window.ResourceDeduplicator({\n          enabled: this.config.enabled,\n          dedupJS: !cspBlock && this.config.jsReplace,\n          dedupCSS: !cspBlock && this.config.cssReplace,","timestamp":1777125856705,"rolledBack":false}
{"id":"ch-2026-04-25T14:09:04.166Z-vb5tq2","taskId":"turn-104","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    /**\n     * CSP预检：在替换前检测页面是否禁止加载外部CDN脚本\n     * 通过插入测试script触发securitypolicyviolation事件\n     */\n    async _precheckCSP() {\n      // 同步检查meta标签CSP\n      const metaCSP = document.querySelector('meta[http-equiv=\"Content-Security-Policy\"]');\n      if (metaCSP) {\n        const content = metaCSP.getAttribute('content') || '';\n        const scriptSrc = content.match(/script-src\\s+([^;]+)/);\n        if (scriptSrc && !/https?:|\\*\\./.test(scriptSrc[1])) {\n          this._cspRestricted = true;\n          ","after":"    /**\n     * CSP预检：在替换前检测页面是否禁止加载外部CDN脚本\n     * 核心思路：插入测试script，等securitypolicyviolation事件判定\n     */\n    async _precheckCSP() {\n      // 同步检查meta标签CSP\n      const metaCSP = document.querySelector('meta[http-equiv=\"Content-Security-Policy\"]');\n      if (metaCSP) {\n        const content = metaCSP.getAttribute('content') || '';\n        const scriptSrc = content.match(/script-src\\s+([^;]+)/);\n        if (scriptSrc && !/https?:|\\*\\./.test(scriptSrc[1])) {\n          this._cspRestricted = true;\n     ","timestamp":1777126144217,"rolledBack":false}
{"id":"ch-2026-04-25T14:09:47.546Z-xebspd","taskId":"turn-106","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"      // 内部状态\n      this._observer = null;\n      this._originalCreateElement = null;\n      this._processedScripts = new WeakSet();","after":"      // 内部状态\n      this._observer = null;\n      this._originalCreateElement = null;\n      this._processedScripts = new WeakMap(); // script → originalSrc","timestamp":1777126187594,"rolledBack":false}
{"id":"ch-2026-04-25T14:10:16.665Z-uec2sk","taskId":"turn-108","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"      // 避免重复处理\n      if (this._processedScripts.has(script)) return;\n      this._processedScripts.add(script);","after":"      // 避免重复处理\n      if (this._processedScripts.has(script)) return;\n\n      const originalSrc = script.src;\n      this._processedScripts.set(script, originalSrc);","timestamp":1777126216671,"rolledBack":false}
{"id":"ch-2026-04-25T14:10:29.807Z-lxvdwm","taskId":"turn-109","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"      // 执行替换 + CDN降级链 + 最终回退\n      try {\n        const originalSrc = script.src;\n        // 构建降级链: 主CDN → fallback CDNs → 原URL\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);","after":"      // 执行替换 + CDN降级链 + 最终回退\n      try {\n        // 构建降级链: 主CDN → fallback CDNs → 原URL\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);","timestamp":1777126229878,"rolledBack":false}
{"id":"ch-2026-04-25T14:10:46.700Z-r0y242","taskId":"turn-111","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\js-replacer.js","action":"modify","before":"    /**\n     * 销毁模块\n     */","after":"    /**\n     * 恢复所有已替换脚本的原始URL\n     */\n    restoreAll() {\n      document.querySelectorAll('script[src]').forEach(script => {\n        const original = this._processedScripts.get(script);\n        if (original && script.src !== original) {\n          script.src = original;\n          console.log(`${LOG_PREFIX} 恢复: ${original}`);\n        }\n      });\n      this._processedScripts = new WeakMap();\n    }\n\n    /**\n     * 销毁模块\n     */","timestamp":1777126246705,"rolledBack":false}
{"id":"ch-2026-04-25T14:11:57.311Z-4jyd4y","taskId":"turn-115","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\font-replacer.js","action":"modify","before":"      // 内部状态\n      this._observer = null;\n      this._processedLinks = new WeakSet();\n\n      console.log(`${LOG_PREFIX} 模块初始化完成`);","after":"      // 内部状态\n      this._observer = null;\n      this._processedLinks = new WeakMap(); // link → originalHref\n\n      console.log(`${LOG_PREFIX} 模块初始化完成`);","timestamp":1777126317317,"rolledBack":false}
{"id":"ch-2026-04-25T14:12:20.745Z-uby0ir","taskId":"turn-117","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\font-replacer.js","action":"modify","before":"      // 避免重复处理\n      if (this._processedLinks.has(link)) return;\n      this._processedLinks.add(link);","after":"      // 避免重复处理\n      if (this._processedLinks.has(link)) return;\n      const originalHref = link.href;\n      this._processedLinks.set(link, originalHref);","timestamp":1777126340751,"rolledBack":false}
{"id":"ch-2026-04-25T14:12:36.715Z-nj5ibw","taskId":"turn-118","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\font-replacer.js","action":"modify","before":"      // 执行替换 + CDN降级链 + 最终回退\n      try {\n        const originalHref = link.href;\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);","after":"      // 执行替换 + CDN降级链 + 最终回退\n      try {\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);","timestamp":1777126356720,"rolledBack":false}
{"id":"ch-2026-04-25T14:12:47.696Z-1njz8x","taskId":"turn-119","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\font-replacer.js","action":"modify","before":"      this._processedLinks = new WeakSet();\n","after":"      this._processedLinks = new WeakMap();\n","timestamp":1777126367702,"rolledBack":false}
{"id":"ch-2026-04-25T14:13:18.630Z-9jnlnq","taskId":"turn-121","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\font-replacer.js","action":"modify","before":"    /**\n     * 销毁模块\n     */","after":"    /**\n     * 恢复所有已替换的原始URL\n     */\n    restoreAll() {\n      document.querySelectorAll('link[rel=\"stylesheet\"]').forEach(link => {\n        const original = this._processedLinks.get(link);\n        if (original && link.href !== original) {\n          link.href = original;\n        }\n      });\n      this._processedLinks = new WeakMap();\n    }\n\n    /**\n     * 销毁模块\n     */","timestamp":1777126398635,"rolledBack":false}
{"id":"ch-2026-04-25T14:13:54.914Z-86u3go","taskId":"turn-124","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\css-accelerator.js","action":"modify","before":"      // 内部状态\n      this._observer = null;\n      this._processedLinks = new WeakSet();\n\n      console.log(`${LOG_PREFIX} 模块初始化完成`);","after":"      // 内部状态\n      this._observer = null;\n      this._processedLinks = new WeakMap(); // link → originalHref\n\n      console.log(`${LOG_PREFIX} 模块初始化完成`);","timestamp":1777126434919,"rolledBack":false}
{"id":"ch-2026-04-25T14:14:16.649Z-om5h5u","taskId":"turn-126","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\css-accelerator.js","action":"modify","before":"      if (this._processedLinks.has(link)) return;\n      this._processedLinks.add(link);","after":"      if (this._processedLinks.has(link)) return;\n      const originalHref = link.href;\n      this._processedLinks.set(link, originalHref);","timestamp":1777126456654,"rolledBack":false}
{"id":"ch-2026-04-25T14:14:28.644Z-oie3e1","taskId":"turn-127","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\css-accelerator.js","action":"modify","before":"      try {\n        const originalHref = link.href;\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);","after":"      try {\n        const fallbacks = (match.fallbackUrls || []).map(f => f.url);","timestamp":1777126468649,"rolledBack":false}
{"id":"ch-2026-04-25T14:14:41.414Z-vf97m7","taskId":"turn-128","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\css-accelerator.js","action":"modify","before":"      this._processedLinks = new WeakSet();","after":"      this._processedLinks = new WeakMap();","timestamp":1777126481419,"rolledBack":false}
{"id":"ch-2026-04-25T14:15:03.117Z-xmbwmt","taskId":"turn-130","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\css-accelerator.js","action":"modify","before":"    destroy() {","after":"    restoreAll() {\n      document.querySelectorAll('link[rel=\"stylesheet\"]').forEach(link => {\n        const original = this._processedLinks.get(link);\n        if (original && link.href !== original) {\n          link.href = original;\n        }\n      });\n      this._processedLinks = new WeakMap();\n    }\n\n    destroy() {","timestamp":1777126503124,"rolledBack":false}
{"id":"ch-2026-04-25T14:16:05.270Z-k6q3xw","taskId":"turn-134","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"modify","before":"    _disableAllModules() {\n      if (this._cspDisabled) return;\n      this._cspDisabled = true;\n\n      console.warn(`${LOG_PREFIX} CSP限制，禁用所有资源替换`);\n      Object.values(this.modules).forEach(m => {\n        if (m && typeof m.destroy === 'function') m.destroy();\n      });\n      this.modules = {\n        jsReplacer: null, fontReplacer: null, cssAccelerator: null,\n        imageOptimizer: null, preloader: null, deduplicator: null\n      };\n\n      if (this._cspListener) {\n        document.removeEventLis","after":"    _disableAllModules() {\n      if (this._cspDisabled) return;\n      this._cspDisabled = true;\n\n      console.warn(`${LOG_PREFIX} CSP限制，恢复原始资源并禁用所有替换`);\n      // 先恢复原始URL，再销毁\n      Object.values(this.modules).forEach(m => {\n        if (m && typeof m.restoreAll === 'function') m.restoreAll();\n      });\n      Object.values(this.modules).forEach(m => {\n        if (m && typeof m.destroy === 'function') m.destroy();\n      });\n      this.modules = {\n        jsReplacer: null, fontReplacer: null, cssAc","timestamp":1777126565275,"rolledBack":false}
{"id":"ch-2026-04-25T14:34:01.497Z-qtwlvq","taskId":"turn-148","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\resource-accelerator.js","action":"create","before":null,"after":"/**\n * 资源加速器主模块 (v2)\n * 1个共享MutationObserver + 按需分发子模块\n * 子模块: JS替换、字体替换、图片/视频懒加载\n */\n\n(function () {\n  'use strict';\n\n  const LOG_PREFIX = '[ResourceAccelerator]';\n  const CONFIG_KEY = 'resourceAcceleratorConfig';\n  const STATS_KEY = 'resourceAcceleratorStats';\n\n  const DEFAULT_CONFIG = {\n    enabled: true,\n    jsReplace: true,\n    fontReplace: true,\n    imageLazyLoad: true,\n    lazyLoadThreshold: 200,\n    excludeDomains: [],\n    excludeUrls: []\n  };\n\n  class ResourceAccelerator {\n    construct","timestamp":1777127641546,"rolledBack":false}
{"id":"ch-2026-04-25T14:34:58.292Z-f7swvn","taskId":"turn-151","filePath":"E:\\code\\chrome-extension-template\\shared\\cdn-mappings.js","action":"modify","before":"  // ========== CDN 健康探测 ==========\n\n  const CDNHealthProbe = {\n    // 缓存: { cdnId: { healthy, latency, timestamp } }\n    _cache: {},\n    TTL: 5 * 60 * 1000, // 5分钟\n    TIMEOUT: 3000, // 3秒超时\n    _pending: {}, // 防止重复探测\n\n    /**\n     * 探测单个CDN可用性\n     */\n    async probe(cdnId) {\n      const now = Date.now();\n      const cached = this._cache[cdnId];\n\n      // 缓存有效\n      if (cached && (now - cached.timestamp) < this.TTL) {\n        return cached;\n      }\n\n      // 防止并发重复探测\n      if (this._pending[c","after":"  // ========== 匹配方法 ==========","timestamp":1777127698299,"rolledBack":false}
{"id":"ch-2026-04-25T14:35:32.029Z-ivhc4m","taskId":"turn-152","filePath":"E:\\code\\chrome-extension-template\\shared\\cdn-mappings.js","action":"modify","before":"  /**\n   * 按CDN降级链构建URL(健康探测 + 备选URL)\n   * 返回 { url, cdnId, fallbackUrls }\n   */\n  function tryCDNChain(cdnOrder, config, version) {\n    const healthy = CDNHealthProbe.getHealthy(cdnOrder);\n    const healthyIds = healthy.map(h => h.id);\n    // 合并：健康CDN优先，未知状态其次，不健康放最后\n    const ordered = [...new Set([...healthyIds, ...cdnOrder])];\n    const fallbackUrls = [];\n    let primary = null;\n\n    for (const cdnId of ordered) {\n      const cdn = CDN_BY_ID[cdnId];\n      if (!cdn || cdn._disabled) continue;","after":"  /**\n   * 按CDN降级链构建URL + 备选URL\n   * 返回 { url, cdnId, fallbackUrls }\n   */\n  function tryCDNChain(cdnOrder, config, version) {\n    const urls = [];\n\n    for (const cdnId of cdnOrder) {\n      const cdn = CDN_BY_ID[cdnId];\n      if (!cdn || cdn._disabled) continue;\n      const url = buildCDNUrl(cdn, config, version, config.file);\n      if (url) urls.push({ url, cdnId });\n    }\n\n    if (urls.length === 0) return null;\n\n    const [primary, ...fallbacks] = urls;\n    primary.fallbackUrls = fallbacks;\n","timestamp":1777127732035,"rolledBack":false}
{"id":"ch-2026-04-25T14:36:44.727Z-7nd2je","taskId":"turn-157","filePath":"E:\\code\\chrome-extension-template\\shared\\cdn-mappings.js","action":"modify","before":"  window.CDNMappings = {\n    JS_CDN_MAP,\n    CSS_CDN_MAP,\n    FONT_CDN_MAP,\n    CDN_SOURCES,\n    CDN_BY_ID,\n    CDNHealthProbe,\n    extractVersion,\n    matchJSLibrary,\n    matchCSS,\n    matchFont\n  };","after":"  window.CDNMappings = {\n    JS_CDN_MAP,\n    CSS_CDN_MAP,\n    FONT_CDN_MAP,\n    CDN_SOURCES,\n    CDN_BY_ID,\n    extractVersion,\n    matchJSLibrary,\n    matchCSS,\n    matchFont\n  };","timestamp":1777127804734,"rolledBack":false}
{"id":"ch-2026-04-25T14:37:14.694Z-33uyb6","taskId":"turn-159","filePath":"E:\\code\\chrome-extension-template\\content\\entries\\core.js","action":"modify","before":"// 10. 资源加速器（最优先加载 — document_start 时尽早拦截资源）\nimport '../../shared/cdn-mappings.js';\nimport '../modules/js-replacer.js';\nimport '../modules/font-replacer.js';\nimport '../modules/css-accelerator.js';\nimport '../modules/image-optimizer.js';\nimport '../modules/resource-preloader.js';\nimport '../modules/resource-deduplicator.js';\nimport '../modules/resource-accelerator.js';","after":"// 10. 资源加速器（最优先加载 — document_start 时尽早拦截资源）\nimport '../../shared/cdn-mappings.js';\nimport '../modules/resource-accelerator.js';","timestamp":1777127834699,"rolledBack":false}
{"id":"ch-2026-04-25T14:38:15.308Z-3fetid","taskId":"turn-161","filePath":"E:\\code\\chrome-extension-template\\content\\modules\\index.js","action":"modify","before":"    // 资源加速器模块\n    CDNMappings: 'shared/cdn-mappings.js',\n    JSReplacer: 'content/modules/js-replacer.js',\n    FontReplacer: 'content/modules/font-replacer.js',\n    CSSAccelerator: 'content/modules/css-accelerator.js',\n    ImageOptimizer: 'content/modules/image-optimizer.js',\n    ResourcePreloader: 'content/modules/resource-preloader.js',\n    ResourceDeduplicator: 'content/modules/resource-deduplicator.js',\n    ResourceAccelerator: 'content/modules/resource-accelerator.js'","after":"    // 资源加速器模块\n    CDNMappings: 'shared/cdn-mappings.js',\n    ResourceAccelerator: 'content/modules/resource-accelerator.js'","timestamp":1777127895315,"rolledBack":false}
